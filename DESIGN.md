# AI몬스터 (AIMON) — 설계서 v0.1

> 초등 5~6학년 대상 AI 윤리 교육 게임
> 바닐라 JS + Canvas / 빌드 없음 / 오프라인 실행

---

## 0. 한 줄 요약

포켓몬의 "만나서 → 약하게 만들고 → 잡는다" 루프를 그대로 쓰되,
**공격 = 상황 문제 풀기**, **포획 = 문제를 이해했다는 증명**으로 바꾼 게임.

---

## 1. 세계관

AI가 널리 쓰이는 세상. 사람들이 무심코 저지르는 잘못이 **그림자몬**이 되어 떠돈다.
주인공은 그림자몬을 만나 상황 문제를 풀며 정체를 밝히고, **가치볼**로 봉인한 뒤
**정화(淨化)** 시켜 **가치몬**으로 도감에 등록한다.

```
야생 그림자몬 조우
      ↓
판단 도구 선택 → 상황 문제 출제 → 정답/오답
      ↓ (장악력 30% 이하 + 정답률 60% 이상)
가치볼 투척 → 흔들림 3회 → 포획
      ↓
정화 연출 → 가치몬으로 진화 → 도감 등록
```

**핵심 교육 설계**: 문제(그림자몬)만 가르치지 않고, 반드시 **올바른 대안(가치몬)을 짝으로** 가르친다.
"하지 마라"가 아니라 "대신 이렇게 하라"가 도감에 남는다.

---

## 2. 판단 도구 (= 기술 4개)

플레이어가 매 턴 고르는 카드. 고른 도구의 태그가 붙은 문항이 출제된다.

| 도구 | 뜻 | 강함 ×1.5 | 약함 ×0.5 |
|---|---|---|---|
| 🔍 **진짜일까?** | 사실인지, 어디서 왔는지 따져본다 | 허위정보 | 저작권 |
| ⚖️ **누구의 것일까?** | 주인이 있는지, 있다면 누구인지 본다 | 저작권 | 개인정보 |
| 🤝 **누가 다칠까?** | 내 행동으로 누가 영향받을지 헤아린다 | 개인정보 | 허위정보 |
| 🧠 **나는 왜 이걸 하려 하지?** | 내 마음속 진짜 이유를 본다 | 전부 ×1.0 | 없음 (오답 피해 절반) |

> **이름을 답이 아니라 질문으로 둔 이유**
> 처음에는 출처확인·권리존중·책임사용처럼 결론을 이름으로 썼다.
> 그러면 도구를 고르는 순간 답의 모양을 통보받는다.
> "출처확인"을 골랐으면 정답은 당연히 확인하는 것이 된다.
> 질문으로 바꾸면 무엇을 따져볼지만 정해지고 답은 알려주지 않는다.
> 생각을 대신해 주지 않는 것이 이 게임이 하려는 일이다.

앞의 3개는 **가위바위보 순환**이라 상성 학습이 직관적이다.
🧠 "나는 왜 이걸 하려 하지?" 는 "안전패" — 배율은 안 오르지만 틀려도 덜 다친다.
자기 동기를 돌아보는 일은 틀려도 벌할 것이 아니기 때문이다.

이 질문은 나머지 셋과 층위가 다르다. 정직·공감·신뢰·공정처럼
앞의 셋으로 이름 붙기 어려운 것들이 여기로 들어온다.

---

## 3. 몬스터 6종 (3속성 × 2종)

### 이름 규칙

| | 규칙 | 뜻 |
|---|---|---|
| 그림자몬 | **나쁜 행동 + 몬** | 나쁜 놈들이니까 전부 `-몬` |
| 가치몬 | **좋은 행동 + 지기** | 문지기·등대지기의 그 `지기` — 지킨다는 뜻 |

**예외 없음.** 보스도 `-몬`이다.
짝은 반드시 **반대말**로 맞춘다 — 정화 연출이 "행동이 뒤집히는 순간"으로 읽혀야 한다.

| Lv | 그림자몬 | → | 가치몬 | 대비 |
|---|---|---|---|---|
| 5 | 복붙몬 | → | 출처지기 | 복붙 ↔ 출처 |
| 6 | 막올림몬 | → | 동의지기 | 막 올림 ↔ 동의 |
| 7 | 그럴싸몬 | → | 확인지기 | 그럴싸 ↔ 확인 |
| 8 | 슬쩍몬 | → | 허락지기 | 슬쩍 ↔ 허락 |
| 9 | 술술몬 | → | 비밀지기 | 술술 ↔ 비밀 |
| 10 | 가짜몬 | → | 진실지기 | 가짜 ↔ 진실 |

---

### ⚖️ 저작권 계열

**1. 복붙몬** — Lv.5 / 장악력 60
- 인터넷에 있는 글을 통째로 복사해 자기가 쓴 것처럼 숙제로 낸다.
- 생김새: 종이 두 장이 겹쳐진 몸통, 눈은 `Ctrl` `V` 두 글자.
- 정화 → **출처지기** : 내 말로 다시 쓰고 가져온 곳을 밝히는 습관.

**2. 슬쩍몬** — Lv.8 / 장악력 90
- 워터마크를 지우고 이미지·글꼴·음악을 슬쩍 가져다 쓴다.
- 생김새: 지우개 형태, 몸에 반쯤 지워진 워터마크 무늬.
- 정화 → **허락지기** : **만든 사람**이 정한 이용 조건을 확인하는 습관.

### 🤝 개인정보 계열

**3. 막올림몬** — Lv.6 / 장악력 70
- 친구 사진을 허락도 안 받고 막 올리거나 AI에 집어넣는다.
- 생김새: 액자 몸통, 안에 흐릿하게 지워진 얼굴.
- 정화 → **동의지기** : 올리기 전에 **찍힌 사람**에게 물어보는 습관.

**4. 술술몬** — Lv.9 / 장악력 100
- 챗봇에 이름·학교·주소·전화번호를 술술 적어 넣는다.
- 생김새: 입력창 모양 몸통에서 글자가 줄줄 새어나옴.
- 정화 → **비밀지기** : 꼭 필요한 정보만 최소한으로 넣는 습관.

### 🔍 허위정보 계열

**5. 그럴싸몬** — Lv.7 / 장악력 80
- AI가 지어낸 거짓말을 그럴싸하게 꾸며 진짜처럼 퍼뜨린다.
- 생김새: 말풍선 몸통, 안의 글자가 계속 바뀜.
- 정화 → **확인지기** : 두 군데 이상에서 교차 확인하는 습관.

**6. 가짜몬** — Lv.10 / 장악력 120 *(최종 보스급)*
- 딥페이크 영상과 가짜 뉴스로 사람을 속인다.
- 생김새: 얼굴이 두 개로 갈라져 어긋나 있는 형태.
- 정화 → **진실지기** : 진짜와 가짜를 가려내는 눈.

### 헷갈림 방지 — 허락지기 vs 동의지기

이름이 비슷한 건 **의도한 것**이다. 둘 다 "남의 것에 함부로 손대지 않는다"는 같은 뿌리다.
차이는 **누구에게 구하느냐**이고, 이게 곧 저작권과 초상권의 차이다.

- **허락지기** → **만든 사람**에게 (저작권)
- **동의지기** → **찍힌 사람**에게 (초상권)

헷갈리는 지점을 그대로 학습 포인트로 쓴다.

---

## 4. 전투 수치

| 항목 | 값 |
|---|---|
| 플레이어 신뢰도 | 100 |
| 기본 데미지 | 25 |
| 최종 데미지 | `25 × 상성배율 × 콤보배율` |
| 콤보배율 | 2연속 ×1.2 / 3연속 ×1.4 / 4연속 이상 ×1.6 |
| 오답 피해 | 신뢰도 −15 (🧠 "나는 왜 이걸 하려 하지?" 선택 시 −8) |
| 오답 시 콤보 | 초기화 |

### 포획 조건
장악력 **30% 이하** **그리고** 누적 정답률 **60% 이상**일 때만 가치볼을 던질 수 있다.
→ "충분히 이해했을 때만 잡을 수 있다"는 규칙이 곧 학습 목표다.

### 포획 확률 (PokeRogue 공식 변형)

```
M = 최대 장악력, H = 현재 장악력
base   = (3M − 2H) / 3M
P      = base × 볼배율 × 정답률배율 × 콤보보너스
P      = min(P, 0.95)
```

- 볼배율: 가치볼 ×1.0 / 근거볼 ×1.5 / 확신볼 ×2.5
- 정답률배율: 정답률 0.6 → ×1.0, 0.8 → ×1.3, 1.0 → ×1.6
- 콤보보너스: 마지막 3문제 연속 정답 시 ×1.2

### 흔들림 연출 — 1세대 방식 채택

FullScreenPokemon의 `Equations.numBallShakes()`가 쓰는 **"흔들림 횟수를 먼저 정하고 보여주는"** 방식이
매 회 주사위를 굴리는 방식보다 연출이 훨씬 극적이다. 이쪽을 쓴다.

```
성공  → 흔들림 3회 후 "잡았다!"
실패  → P 값에 따라 흔들림 횟수를 미리 결정
        P ≥ 0.60 → 3회 흔들리고 탈출  (아깝다!)
        P ≥ 0.35 → 2회
        P ≥ 0.15 → 1회
        그 미만  → 0회 (바로 튀어나옴)
```

"세 번 흔들리다 튀어나왔다"는 아쉬움이 곧 **"조금만 더 풀면 잡겠다"**는 학습 동기가 된다.

---

## 5. 문항 스펙

### 구조 — 속성별 공유 풀
몬스터마다 따로 만들지 않고 **속성별 10문항 풀**을 만든다.
같은 속성의 두 몬스터가 풀을 공유하되, `level`로 난이도를 나눈다.

```
저작권 풀   10문항  ─┬─ 복붙몬(Lv.5)   → easy 문항 우선
                    └─ 몰래쓱(Lv.8)   → hard 문항 우선
개인정보 풀 10문항  ─┬─ 얼굴올림(Lv.6)
                    └─ 술술이(Lv.9)
허위정보 풀 10문항  ─┬─ 그럴싸(Lv.7)
                    └─ 진짜같아(Lv.10)
────────────────────────────────
합계        30문항
```

각 풀 10문항의 도구 태그 배분: 🔍3 / ⚖️3 / 🤝2 / 🧠2

### 데이터 형식

```js
{
  id: "cr_01",
  type: "copyright",           // copyright | privacy | disinfo
  tool: "respect",             // verify | respect | ownership | critique
  level: "easy",               // easy | hard
  situation: "모둠 발표 자료를 만들다가 인터넷에서 딱 맞는 문단을 찾았다.",
  question: "이럴 때 가장 알맞은 행동은?",
  options: [
    "그대로 복사해서 붙여 넣는다.",
    "몇 단어만 바꿔서 붙여 넣는다.",
    "내 말로 다시 정리하고 어디서 봤는지 밝힌다.",
    "출처만 적고 통째로 복사해 넣는다."
  ],
  answer: 2,
  explanation: "남의 글은 그대로도, 몇 글자 바꿔서도 내 글이 되지 않아요. 내 말로 다시 쓰고 출처를 밝히는 게 올바른 방법이에요.",
  wrongHint: "출처를 적었다고 해서 통째로 베껴도 되는 건 아니에요."
}
```

`situation` / `explanation` / `wrongHint`가 실제 수업 자료가 된다.
**오답일 때도 반드시 해설을 보여준다** — 틀린 것도 학습이다.

### ★ 틀린 문항 되돌려 넣기

**맞힌 문항은 그 판에서 빠지고, 틀린 문항은 풀로 돌아온다.**
해설을 방금 읽었으니 다시 만나 맞히고 넘어가라는 뜻이다.

이 규칙이 없으면 못 맞히는 아이일수록 문항이 빨리 떨어져서
**실력이 아니라 문제가 다 떨어져서 지게 된다.** 도움이 가장 필요한 아이에게만
일어나는 실패라 교육 게임에서 가장 나쁜 방향이다.

시뮬레이션 5,000판 기준 (도구를 아무거나 고르는 아이):

| 정답률 | | 문항 소진 | 포획 성공 |
|---|---|---|---|
| 55% | 규칙 없음 | 25~34% | 64~73% |
| 55% | **규칙 적용** | **4~8%** | **79~85%** |
| 75% | 규칙 적용 | 0.3~0.4% | 99.5% |

실패의 원인이 "문항 재고"에서 **"신뢰도 부족"**으로 옮겨갔다. 이게 옳은 방향이다.

### 문항 수는 30개로 충분하다

늘려야 하는지 재봤다. 전체 6판을 한 번 플레이할 때:

| 풀 크기 | 정답률 | 본 문항 | **한 번도 안 나온 문항** | 한 문항 최다 반복 |
|---|---|---|---|---|
| **30개** | 75% | 19.7 | **10.3개** | 2.4회 |
| **30개** | 55% | 25.0 | 5.0개 | 3.7회 |
| 42개 | 75% | 22.4 | 19.6개 | 1.9회 |
| 42개 | 55% | 31.8 | 10.2개 | 3.1회 |

모자라는 게 아니라 **남는다.** 잘하는 아이는 30개 중 10개를 아예 못 보고 끝낸다.
42개로 늘려도 반복은 3.7 → 3.1회로 거의 안 줄고, 안 쓰는 문항만 두 배가 된다.
반복의 원인은 풀 크기가 아니라 **되돌려 넣기 규칙 자체**이고, 그건 의도한 학습 기능이다.

→ 30개 유지. 남는 문항은 **다시 플레이할 이유**가 된다.

### 판 사이 중복 줄이기
같은 속성의 두 몬스터가 풀을 나눠 쓰므로, 두 번째 판에서는
**이번 게임에 아직 안 나온 문항을 먼저** 꺼낸다 (`pickQuestion` 의 `seenIds`).

### 별도 파일로 분리
`data/questions.js` 한 파일에 모아 선생님이 학년·상황에 맞게 갈아끼울 수 있게 한다.
기존 문해력 진단 앱의 `questionData` 구조를 계승.

---

## 6. 화면 구성

```
① 시작       타이틀 + 이름 입력 + 도감 보기
② 맵         타일맵 이동(방향키/터치), 데이터 풀숲에서 랜덤 조우
③ 전투       상단 몬스터/장악력, 하단 신뢰도 + 도구 4버튼
④ 문제       상황 → 질문 → 4지선다 → 정답 판정 + 해설
⑤ 포획       가치볼 선택 → 던지기 → 흔들림 3회 → 성공/실패
⑥ 정화       그림자몬 → 가치몬 변신 연출 + 도감 등록
⑦ 도감       6칸 그리드, 잡은 것만 공개, 클릭 시 상세
⑧ 리포트     속성별 정답률 + 진단 문구 + 인쇄 (기존 app.js 로직 재활용)
```

---

## 7. 기술 구조

모듈 분할은 **FullScreenPokemon의 `src/sections/` 방식**을 따른다.
기능별로 파일을 자르는 이 구조가 우리 규모에 딱 맞다.
(PokeRogue의 "페이즈 98개" 방식은 이 규모에 과하다.)

```
06_AI몬스터/
├─ index.html          단일 진입점
├─ style.css
├─ src/
│  ├─ main.js          게임 루프 + 화면 상태머신
│  ├─ render.js        Canvas 픽셀 렌더러 (팔레트 + 인덱스 격자)
│  ├─ menus.js         대화창 + 선택지 (선언형 스키마)
│  ├─ maps.js          타일맵 + 이동 + 충돌 + 랜덤 조우
│  ├─ battle.js        전투 흐름 (조우→선택→출제→판정)
│  ├─ equations.js     데미지·상성·포획확률·흔들림 계산 전담
│  ├─ cutscene.js      정화 연출 등 스크립트형 연출
│  ├─ dex.js           도감 + localStorage 저장
│  └─ report.js        결과 리포트 + 인쇄
├─ data/
│  ├─ monsters.js      몬스터 6종 정의 + 스프라이트 격자
│  ├─ questions.js     문항 30개
│  ├─ tools.js         판단 도구 + 상성표
│  └─ maps/            맵 데이터 (연구소, 데이터숲, ...)
└─ DESIGN.md
```

**`equations.js`를 따로 빼는 게 핵심.** FullScreenPokemon이 모든 수식을
`Equations.ts` 한 곳에 모아둔 덕에 밸런스 조정이 파일 하나로 끝난다.
교실에서 "너무 어렵다/쉽다"는 피드백이 오면 여기만 고친다.

`menus.js`는 MenuGrapher 패턴 차용 — 대화창을 코드가 아니라 **데이터로 기술**한다.
그래야 선생님이 대사를 직접 고칠 수 있다.

### 픽셀 아트 방식
스프라이트를 **팔레트 + 인덱스 격자 배열**로 JS에 직접 넣는다.

```js
palette: ["transparent", "#f8f8f0", "#181818", "#c7c7c0", "#808080"],
grid: [
  [0,0,2,2,2,2,0,0],
  [0,2,1,3,3,1,2,0],
  ...
]
```

**팔레트는 4색 그레이스케일 램프**를 기본으로 한다 — 스크린샷의 게임보이 화면과 같은 계열이고,
FullScreenPokemon도 `Graphics.ts`에서 동일한 5칸(투명 포함) 구성을 쓴다.
속성별 강조색(저작권=파랑 / 개인정보=주황 / 허위정보=보라)만 한 칸 얹어 구분한다.

- 외부 이미지 파일 0개 → `file://` 더블클릭으로 실행, 교실 오프라인 환경 대응
- `ctx.imageSmoothingEnabled = false` + **정수배 확대**(×3 또는 ×4)로 선명한 픽셀 유지
- 색 하나 바꾸는 수정이 배열 한 칸 수정
- **전부 오리지널** — 저작권 교육 게임이 저작권을 지킨다

---

## 8. 저작권 원칙 (이 프로젝트의 자기 규율)

`참고 게임/` 아래 두 프로젝트는 **설계 구조 참고용으로만** 본다.

| 항목 | 방침 |
|---|---|
| PokeRogue 코드 | 복사하지 않음. **AGPL-3.0**이라 가져오면 우리 게임도 전체 공개 의무 발생 |
| FullScreenPokemon 코드 | 코드 자체는 MIT라 인용은 가능하나, **복사하지 않고 구조만 참고** |
| 두 프로젝트의 포켓몬 스프라이트·이름·데이터 | 일절 사용하지 않음 (닌텐도/게임프리크 저작물, MIT 적용 대상 아님) |
| 포획 확률·흔들림 공식 | 수학적 아이디어라 참고 가능. 수치는 우리 게임에 맞게 전부 재설계 |
| 4색 그레이스케일 팔레트 | 하드웨어에서 온 색 구성이라 보호 대상 아님 |
| 게임 장르 문법 | 조우–전투–포획 루프는 장르 관습, 보호 대상 아님 |
| 픽셀 아트·몬스터 이름·문항·대사 | **100% 신규 창작** |

수업에서 "이 게임도 남의 것을 베끼지 않고 만들었다"고 말할 수 있어야 한다.
이 표 자체가 수업 자료가 된다 — "무엇은 되고 무엇은 안 되는지"의 실제 사례.

---

## 8-1. 참고 게임 비교

| | **FullScreenPokemon** | **PokeRogue** |
|---|---|---|
| 정체 | 1세대(적/녹) HTML5 리메이크 | 로그라이크 팬게임 |
| 스택 | TypeScript + EightBittr 엔진 | TypeScript + Phaser 3 |
| 규모 | 276 파일 | 1,342 파일 |
| 라이선스 | MIT (코드) | **AGPL-3.0** |
| 실행 가능? | 예 | **아니오** (에셋이 빈 submodule) |
| 화면 | 스크린샷과 동일한 게임보이 화면 | 현대적 고해상도 |
| **참고 가치** | **높음** | 중간 |

### FullScreenPokemon에서 가져올 것

| 파일 | 얻는 것 |
|---|---|
| `sections/Equations.ts` | 모든 수식을 한 파일에 모으는 설계 + 흔들림 횟수 계산 |
| `sections/Graphics.ts` | 4색 팔레트 + 정수배 확대 렌더링 |
| `sections/menus/` + `createMenuGrapher.ts` | 대화창·선택지를 데이터로 기술하는 패턴 |
| `sections/cutscenes/` (16개) | 연출을 스크립트로 기술 → 정화 연출에 적용 |
| `creators/mapsLibrary/` | 맵을 데이터 파일로 분리하는 방식 |
| `sections/` 전체 | 기능별 모듈 분할 — 우리 폴더 구조의 원형 |

### PokeRogue에서 가져올 것
`phases/attempt-capture-phase.ts`의 포획 공식 **아이디어 하나**.
나머지는 규모·라이선스·미실행 문제로 참고 가치가 낮다.

---

## 9. 제작 단계

| 단계 | 산출물 | 상태 |
|---|---|---|
| 0 | 설계서 (이 문서) | ✅ |
| 1 | `data/` 3종 — 몬스터·도구·문항 30개 | ✅ |
| 2 | 픽셀 렌더러 + 스프라이트 12장 + 주인공 6컷 | ✅ |
| 3 | 전투/퀴즈/포획/정화 엔진 | ✅ |
| 4 | 맵 이동 + 랜덤 조우 | ✅ |
| 5 | 도감 + 저장 + 리포트(인쇄) | ✅ |
| 6 | 교실 배포 패키지 + 사용 안내(`README.md`) | ✅ |
| 7 | 학생별 저장 + 반 전체 기록 + 안내 + 소리 | ✅ |

---

## 9-1. 2차 작업 — 교실에서 실제로 쓰기 위한 것들

### 학생별 저장 (`dex.js`)
교실 컴퓨터는 여러 학생이 돌려 쓴다. 한 칸에 한 명분만 저장하면
다음 학생이 앞 사람 기록을 덮어써 버린다.

```
aimon_roster_v1 : { "민서": {…}, "지호": {…} }
aimon_last_v1   : 마지막에 한 사람 이름
```

`save` 는 "지금 하고 있는 학생" 한 명이고, 전투·기록 쪽 코드는 예전 그대로 `save` 만 본다.
이름을 그대로 다시 적으면 이어서 하고, 새 이름이면 새로 시작한다.

**선생님용 반 전체 기록**(`renderClassReport`)은 반 평균과 **반 전체가 어려워한 주제**를
짚어 준다. 다음 수업에서 무엇을 다시 다룰지 정하는 근거가 된다.

### 안내 (`tutorial.js`)
맵에 그냥 떨어뜨려 놓으면 뭘 해야 할지 모른다. 박사님이 딱 세 번만 말을 건다.

| 언제 | 무엇을 |
|---|---|
| 시작할 때 | 무엇을 하는 게임인지, 색깔 풀숲 세 군데로 가라 |
| 첫 전투 | 도구 4개와 상성, 틀려도 다시 나온다는 것 |
| 첫 포획 기회 | 왜 지금 던질 수 있게 됐는지 (장악력 30% + 정답률 60%) |

한 번 본 안내는 그 학생 저장본에 표시해 두고 다시 띄우지 않는다.

### 소리 (`audio.js`)
**음원 파일을 쓰지 않는다.** Web Audio 로 그 자리에서 만든다.
받을 파일이 없으니 오프라인 실행이 그대로 유지되고, 남의 음악을 가져오지
않으니 저작권 원칙(§8)도 그대로 지킨다.

배경음 가락은 **5음 음계(도·레·미·솔·라)** 로 짰다. 어느 음을 겹쳐도 어긋나지 않아
단순한 반복인데도 귀에 거슬리지 않는다.

**배경음은 기본이 꺼짐이다.** 교실에서 컴퓨터 서른 대가 동시에 노래를 틀면 수업이 안 된다.

### 2차 작업에서 검증한 것
- 안내 3종이 각각 제때 뜨고, 한 번 본 뒤엔 안 뜸
- 학생 둘을 만들어 **기록이 섞이지 않음** 확인
  (민서 저작권 2문제·1마리 / 지호 개인정보 2문제·0마리, 서로 침범 없음)
- 반 전체 기록: 반 평균 50%, 주제별 집계, 학생별 표 정확
- 효과음 10종 → 오실레이터 27개 생성 / 끄면 0개 / 배경음 켜고 끄기 정상

> 점검 중 전투 애니메이션이 멈춘 것처럼 보였는데,
> 브라우저 창이 뒤에 있어 탭이 throttle 된 것이었다
> (16ms 간격이 1.5초에 2번만 실행). 게임 버그가 아니다.

---

## 10. 만들면서 바꾼 것 · 잡은 버그

설계와 실제가 어긋난 지점과, 브라우저에서 돌려 보고 고친 것들.

### 설계 변경

**문제 텍스트는 캔버스가 아니라 HTML 로 그린다**
게임보이 해상도(160×144)에 한글 문제 지문이 물리적으로 안 들어가고,
한글 픽셀 폰트는 오프라인에서 구할 수 없다.
→ 픽셀 아트(맵·몬스터·주인공)만 캔버스, 글자는 HTML 을 CSS 로 레트로하게.

**문항 풀의 easy/hard 분리를 없앴다**
10문항을 반으로 쪼개면 몬스터 하나가 5문항만 갖게 되고, 도구 4개로 나누면
도구당 1~2개라 한 판 안에서 문제가 동난다.
→ 난이도는 **장악력**으로 준다. `level` 은 필터가 아니라 선호로만 쓴다.

**포획 곡선을 세제곱으로 바꿨다**
젠1 공식을 그대로 쓰니 던질 수 있게 되는 순간 이미 80% 라서 한 번에 잡히고,
볼 3종이 전부 95% 상한에 걸려 의미가 없었다. 설계서가 약속한
"세 번 흔들리다 튀어나오는 아쉬움" 이 아예 발생하지 않았다.

| 기본볼 | 문턱 30% | 15% | 바닥 |
|---|---|---|---|
| 고치기 전 | 80% | 91% | 100% |
| **세제곱 후** | **51%** | **73%** | **95%** |

근거볼 77% / 확신볼 95% 로 세 볼의 역할이 갈렸고,
정답률 100% 면 문턱에서도 82% 라 잘 푼 아이가 확실히 보상받는다.

### 잡은 버그

**캔버스가 좁은 화면에서 안 줄어듦**
`setupCanvas` 가 인라인 `style.width` 를 박아 CSS 의 `width:100%` 를 덮어썼다.
무대는 712px 인데 캔버스가 640px 에 고정돼 오른쪽 72px 이 검게 남았다.
→ 그리는 픽셀 수(`width` 속성)만 정하고, 보이는 크기는 CSS 에 맡긴다.

**방향 버튼을 누른 채 손을 밖에서 떼면 주인공이 안 멈춤**
`pointerup` 을 버튼에서만 들어서, 손가락을 버튼 밖으로 빼고 떼면
반복 타이머가 안 꺼지고 혼자 계속 걸어갔다. 터치 기기에서 반드시 겪는다.
→ 멈추는 책임을 창 전체가 지게 했다 (`window` 의 pointerup·pointercancel·blur,
`visibilitychange`).

### 검증한 것

- 문법·스프라이트 규격(192줄 전부 16칸)·맵 도달 가능성(세 숲 각 12칸)
- 전투 5개 경로 전부: 포획 성공 / 볼 튕김(3회 흔들림) / 신뢰도 0 / 문항 소진 / 도망
- 조우율 15.5%(설정 14%), 숲별 속성 일치, 약↔강 65:35, 보스 3마리 조건
- 정답률 50% 에서도 6마리 **완주율 100%** (볼 10개 중 6.7개 소모)
- 모바일 375px 가로 스크롤 없음, 키보드·한글자판·방향버튼 입력
