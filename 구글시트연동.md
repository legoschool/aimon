# 구글 시트에 기록 모으기 — 설정 안내

한 번만 설정하면 학생들의 플레이 기록이 선생님 시트에 자동으로 쌓입니다.
**약 10분** 걸립니다.

---

## 먼저 알아 두실 것

이 게임에는 **서버가 없습니다.** GitHub Pages는 파일을 보내주기만 합니다.
그래서 시트에 쓰려면 구글이 주는 무료 "창구"를 하나 만들어야 합니다.

그 창구 주소는 게임 코드 안에 들어가므로 **공개됩니다.** 그래서 이렇게 막았습니다.

| | |
|---|---|
| 학생이 시트 내용을 읽을 수 있나 | **아니요.** 창구는 쓰기만 됩니다 |
| 학생이 다른 반 기록을 볼 수 있나 | **아니요.** 읽으려면 선생님 암호가 필요합니다 |
| 학생이 가짜 기록을 넣을 수 있나 | **가능합니다.** 주소를 알아내면요 |

마지막 것은 막을 방법이 없습니다. 정적 사이트는 비밀을 지킬 수 없기 때문입니다.
교실 규모에서는 감당할 만한 위험이라고 봅니다. 이상한 줄이 보이면 시트에서 지우시면 됩니다.

---

## 1단계 — 시트 준비

기록을 모을 구글 시트를 엽니다. 시트 하단 탭 이름을 **`기록`** 으로 바꿔 주세요.
(다른 이름을 쓰시려면 아래 스크립트의 `SHEET_NAME` 도 같이 바꾸시면 됩니다.)

첫 줄은 비워 두세요. 스크립트가 알아서 제목 줄을 만듭니다.

---

## 2단계 — Apps Script 붙여넣기

시트 상단 메뉴에서 **확장 프로그램 → Apps Script** 를 엽니다.
`Code.gs` 에 있던 내용을 모두 지우고, 아래를 통째로 붙여넣으세요.

```javascript
/* AI몬스터 — 기록 받는 창구
   쓰기만 됩니다. 읽으려면 아래 암호가 필요합니다. */

const SHEET_NAME = '기록';
const TEACHER_PASSWORD = '여기를바꾸세요';   // ← 꼭 바꾸세요!

const HEADERS = [
  '보낸시각', '반', '번호', '닉네임',
  '정화', '증표', '푼문제', '맞힌수', '정답률',
  '저작권', '개인정보', '허위정보',
  '생각열쇠', '틀린문제', '만난횟수'
];

function sheet_() {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  let sh = ss.getSheetByName(SHEET_NAME);
  if (!sh) sh = ss.insertSheet(SHEET_NAME);
  if (sh.getLastRow() === 0) {
    sh.appendRow(HEADERS);
    sh.getRange(1, 1, 1, HEADERS.length).setFontWeight('bold');
    sh.setFrozenRows(1);
  }
  return sh;
}

function json_(obj) {
  return ContentService.createTextOutput(JSON.stringify(obj))
    .setMimeType(ContentService.MimeType.JSON);
}

function pct_(right, asked) {
  return asked > 0 ? Math.round((right / asked) * 100) : '';
}

/* 학생이 기록을 보낼 때 (쓰기 전용) */
function doPost(e) {
  try {
    const d = JSON.parse(e.postData.contents || '{}');
    if (d.action !== 'submit') return json_({ ok: false, error: 'bad action' });

    const s = d.stats || {};
    sheet_().appendRow([
      new Date(),
      String(d.klass || '').slice(0, 20),
      String(d.number || '').slice(0, 10),
      String(d.nick || '').slice(0, 20),
      d.caught | 0, d.badges | 0, d.asked | 0, d.right | 0,
      pct_(d.right | 0, d.asked | 0),
      pct_((s.copyright || {}).right, (s.copyright || {}).asked),
      pct_((s.privacy || {}).right, (s.privacy || {}).asked),
      pct_((s.disinfo || {}).right, (s.disinfo || {}).asked),
      d.hints | 0, d.wrongs | 0, d.battles | 0
    ]);
    return json_({ ok: true });
  } catch (err) {
    return json_({ ok: false, error: String(err) });
  }
}

/* 선생님이 기록을 볼 때 (암호 필요) */
function doGet(e) {
  const p = (e.parameter || {});
  if (p.action !== 'list') return json_({ ok: false, error: 'bad action' });
  if (p.pw !== TEACHER_PASSWORD) return json_({ ok: false, error: 'wrong password' });

  const sh = sheet_();
  const last = sh.getLastRow();
  if (last < 2) return json_({ ok: true, rows: [] });

  const values = sh.getRange(2, 1, last - 1, HEADERS.length).getValues();

  /* 같은 반·번호는 가장 최근 기록만 남긴다 */
  const latest = {};
  values.forEach(function (r) {
    const key = r[1] + '-' + r[2];
    const t = r[0] instanceof Date ? r[0].getTime() : 0;
    if (!latest[key] || t >= latest[key].t) {
      latest[key] = {
        t: t,
        when: r[0] instanceof Date ? Utilities.formatDate(r[0], 'Asia/Seoul', 'MM/dd HH:mm') : '',
        klass: r[1], number: r[2], nick: r[3],
        caught: r[4], badges: r[5], asked: r[6], right: r[7], rate: r[8],
        copyright: r[9], privacy: r[10], disinfo: r[11],
        hints: r[12], wrongs: r[13], battles: r[14]
      };
    }
  });

  const rows = Object.keys(latest).map(function (k) { return latest[k]; });
  rows.sort(function (a, b) {
    if (b.caught !== a.caught) return b.caught - a.caught;
    return (b.rate || 0) - (a.rate || 0);
  });
  return json_({ ok: true, rows: rows });
}
```

**`TEACHER_PASSWORD` 를 꼭 바꾸세요.** 이 암호를 아는 사람만 기록을 볼 수 있습니다.

---

## 3단계 — 배포

Apps Script 화면 오른쪽 위 **배포 → 새 배포** 를 누릅니다.

| 항목 | 고를 것 |
|---|---|
| 유형 | **웹 앱** (톱니바퀴 아이콘에서 선택) |
| 설명 | 아무거나 (예: AI몬스터 기록) |
| 다음 사용자로 실행 | **나** |
| 액세스 권한 | **모든 사용자** |

**배포**를 누르면 권한을 묻습니다. 승인해 주세요.
(구글이 "안전하지 않은 앱" 경고를 낼 수 있습니다. **고급 → 안전하지 않음으로 이동**을 눌러 진행하세요.
선생님이 직접 만든 스크립트라 괜찮습니다.)

끝나면 **웹 앱 URL** 이 나옵니다. `https://script.google.com/macros/s/...../exec` 형태입니다.
이 주소를 복사하세요.

> ⚠️ 스크립트를 고칠 때마다 **배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포**
> 를 해야 반영됩니다. 저장만으로는 안 바뀝니다.

---

## 4단계 — 게임에 주소 넣기

`data/config.js` 를 열어 주소를 붙여넣습니다.

```js
const SHEET_ENDPOINT = "여기에 붙여넣기";
```

파일을 저장하고 커밋·푸시하면 1~2분 뒤 적용됩니다.

```bash
cd "G:/내 드라이브/00_코딩/06_AI몬스터" && git add -A && git commit -m "시트 주소 설정" && git push
```

---

## 5단계 — 확인

1. 게임을 열고 **반·번호·닉네임**을 적어 시작합니다
2. 몬스터를 한 마리 정화합니다
3. 시트를 새로고침하면 줄이 하나 생겨 있습니다

**선생님 화면 보기** — 타이틀 화면 오른쪽 아래 작은 **[선생님]** 을 누르고 암호를 넣으면
반 전체 순위와 주제별 정답률이 표로 나옵니다.

---

## 언제 보내지나요

- 몬스터를 정화할 때마다
- 최종 보스를 정화할 때
- 기록 화면에서 **[시트로 보내기]** 를 누를 때

**같은 반·번호는 가장 최근 것만** 선생님 화면에 나옵니다.
(시트에는 모든 줄이 그대로 쌓이므로 변화 과정도 볼 수 있습니다.)

---

## 개인정보에 대해

이 게임은 **개인정보를 조심하라고 가르치는 게임**입니다.
그래서 이름 대신 **반·번호·닉네임**만 보냅니다. 시트가 새어도 이름은 나가지 않습니다.

수업 시작할 때 아이들에게 한마디 해 주시면 좋겠습니다.

> "여러분이 푼 기록은 선생님 시트로 갑니다.
> 이름은 안 보내고 반·번호·별명만 보내요. 왜 그렇게 했을까요?"

이 질문 자체가 오늘 수업의 좋은 시작이 됩니다.

---

## 안 될 때

**시트에 아무것도 안 쌓여요**
`data/config.js` 의 주소가 `/exec` 로 끝나는지 보세요. `/dev` 면 안 됩니다.
그리고 배포할 때 액세스 권한을 **모든 사용자**로 했는지 확인하세요.

**선생님 화면에서 "암호가 틀렸습니다"**
Apps Script 의 `TEACHER_PASSWORD` 와 입력한 암호가 같은지 보세요.
스크립트를 고쳤다면 **새 버전으로 다시 배포**해야 합니다.

**게임이 느려졌어요**
기록 보내기는 실패해도 게임에 영향을 주지 않도록 만들었습니다.
느리다면 다른 원인입니다. `data/config.js` 에서 `SHEET_ENABLED = false` 로 꺼 보세요.

**연동을 안 쓰고 싶어요**
`data/config.js` 의 `SHEET_ENDPOINT` 를 빈 문자열로 두면 됩니다.
게임은 예전처럼 그 컴퓨터에만 저장하며 잘 돌아갑니다.
