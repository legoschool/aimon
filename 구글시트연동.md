# 구글 시트에 기록 모으기

한 번 설정하면 학생들이 몬스터를 정화할 때마다 기록이 선생님 시트에 한 줄씩 쌓입니다. 처음 설정은 10분쯤 걸리고, 이미 쓰고 계셨다면 코드만 바꿔 넣으면 됩니다(아래 "v4로 바꾸기").

연동은 고르는 기능입니다. 설정하지 않아도 게임은 그 컴퓨터에 기록을 남기며 그대로 돌아갑니다.

---

## 먼저 알아 두실 것

이 게임에는 서버가 없습니다. GitHub Pages는 파일을 보내 주기만 하므로, 시트에 쓰려면 구글이 주는 무료 창구(Apps Script 웹 앱)를 하나 만들어야 합니다. 그 창구 주소는 게임 코드 안에 들어가서 누구나 볼 수 있습니다.

| 물음 | 답 |
|---|---|
| 학생이 시트 내용을 읽을 수 있나 | 없습니다. 창구는 쓰기만 받습니다 |
| 학생이 다른 반 기록을 볼 수 있나 | 없습니다. 읽으려면 선생님 암호가 필요합니다 |
| 학생이 가짜 기록을 넣을 수 있나 | 주소를 알아내면 넣을 수 있습니다 |

마지막 줄은 막을 방법이 없습니다. 정적 사이트는 비밀을 지키지 못합니다. 교실 규모에서는 감당할 만한 위험이라고 판단했고, 이상한 줄이 보이면 시트에서 지우시면 됩니다.

게임은 이름을 보내지 않고 반·번호·별명과 정답률만 보냅니다. 그래도 반과 번호가 붙은 학습 기록은 누구인지 알아볼 수 있는 정보입니다. 만 14세 미만 학생의 정보를 모을 때 학교에 정해진 절차(보호자 안내나 동의)가 있는지 먼저 확인해 주세요.

---

## 처음 설정하기

### 1단계. 시트 준비

기록을 모을 구글 시트를 새로 만들고, 아래쪽 탭 이름을 `기록`으로 바꿉니다. 다른 이름을 쓰려면 2단계 코드의 `SHEET_NAME`도 같이 바꿉니다. 첫 줄은 비워 두세요. 제목 줄은 코드가 만듭니다.

### 2단계. Apps Script 붙여넣기

시트 위쪽 메뉴에서 확장 프로그램 → Apps Script를 엽니다. `Code.gs`에 있던 내용을 모두 지우고 아래 코드를 통째로 붙여넣은 뒤, `TEACHER_PASSWORD` 줄의 암호를 선생님만 아는 말로 바꿉니다.

```javascript
/* AI몬스터 — 기록 받는 창구  (v4)
   쓰기만 됩니다. 읽으려면 아래 암호가 필요합니다.
   v4 에서 바뀐 것: 세 스테이지 아홉 주제와 지금 있는 스테이지를 받습니다.
   예전 시트를 그대로 써도 됩니다. 제목 줄 뒤에 새 칸 이름을 알아서 붙입니다. */

const VERSION = 'v4';
const SHEET_NAME = '기록';
const TEACHER_PASSWORD = '여기를바꾸세요';   // 꼭 바꾸세요! 예전 코드를 쓰셨다면 그 암호를 그대로 옮겨 적으세요.

/* 열 순서. 새 칸은 늘 뒤에 붙입니다 (예전에 쌓인 줄이 밀리지 않게). */
const HEADERS = [
  '보낸시각', '반', '번호', '닉네임',
  '정화', '증표', '푼문제', '맞힌수', '정답률',
  '저작권', '개인정보', '허위정보',
  '생각열쇠', '틀린문제', '만난횟수',
  '스테이지', '편향', '의존', '조작', '감시', '불신', '소외'
];

/* 주제 → 열 번호 (0부터) */
const TOPIC_COL = {
  copyright: 9, privacy: 10, disinfo: 11,
  bias: 16, depend: 17, manipul: 18,
  surveil: 19, distrust: 20, exclude: 21
};

function sheet_() {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  let sh = ss.getSheetByName(SHEET_NAME);
  if (!sh) sh = ss.insertSheet(SHEET_NAME);
  if (sh.getLastRow() === 0) {
    sh.appendRow(HEADERS);
    styleHeader_(sh);
  } else if (sh.getLastColumn() < HEADERS.length) {
    // 예전 창구(v2·v3)가 만든 시트면 제목 줄을 새 칸까지 늘린다 (한 번만 일어난다)
    sh.getRange(1, 1, 1, HEADERS.length).setValues([HEADERS]);
    styleHeader_(sh);
  }
  return sh;
}

function styleHeader_(sh) {
  sh.getRange(1, 1, 1, HEADERS.length).setFontWeight('bold');
  sh.setFrozenRows(1);
}

function json_(obj) {
  obj.version = VERSION;   // 배포가 반영됐는지 눈으로 확인하려고
  return ContentService.createTextOutput(JSON.stringify(obj))
    .setMimeType(ContentService.MimeType.JSON);
}

function pct_(right, asked) {
  return asked > 0 ? Math.round((right / asked) * 100) : '';
}

/* 시각 칸이 날짜가 아니라 글자로 들어와 있을 때도 읽어낸다 */
function when_(v) {
  const d = (v instanceof Date) ? v : new Date(v);
  if (isNaN(d.getTime())) return '';
  return Utilities.formatDate(d, 'Asia/Seoul', 'MM/dd HH:mm');
}
function stamp_(v) {
  const d = (v instanceof Date) ? v : new Date(v);
  return isNaN(d.getTime()) ? 0 : d.getTime();
}

/* 학생이 기록을 보낼 때 (쓰기 전용) */
function doPost(e) {
  try {
    const d = JSON.parse(e.postData.contents || '{}');
    if (d.action !== 'submit') return json_({ ok: false, error: 'bad action' });

    const s = d.stats || {};
    // 반 "5-3" 을 시트가 5월 3일로 바꾸지 않도록 앞에 ' 를 붙인다 (게임이 이미 붙였으면 그대로)
    const text = function (v, n) {
      const t = String(v === undefined || v === null ? '' : v).trim().slice(0, n);
      return t === '' ? '' : (t.charAt(0) === "'" ? t : "'" + t);
    };
    const row = new Array(HEADERS.length).fill('');
    row[0] = new Date();
    row[1] = text(d.klass, 20);
    row[2] = text(d.number, 10);
    row[3] = text(d.nick, 20);
    row[4] = d.caught | 0;
    row[5] = d.badges | 0;
    row[6] = d.asked | 0;
    row[7] = d.right | 0;
    row[8] = pct_(d.right | 0, d.asked | 0);
    Object.keys(TOPIC_COL).forEach(function (k) {
      const t = s[k] || {};
      row[TOPIC_COL[k]] = pct_(t.right | 0, t.asked | 0);
    });
    row[12] = d.hints | 0;
    row[13] = d.wrongs | 0;
    row[14] = d.battles | 0;
    row[15] = d.stage | 0 || '';

    sheet_().appendRow(row);
    return json_({ ok: true });
  } catch (err) {
    return json_({ ok: false, error: String(err) });
  }
}

/* 선생님이 기록을 볼 때 (암호 필요) */
function doGet(e) {
  const p = (e.parameter || {});
  if (p.action !== 'list') return json_({ ok: false, error: 'bad action' });
  if (p.pw !== TEACHER_PASSWORD) return json_({ ok: false, error: 'wrong password' });

  const sh = sheet_();
  const last = sh.getLastRow();
  if (last < 2) return json_({ ok: true, rows: [] });

  const width = Math.min(HEADERS.length, sh.getMaxColumns());
  const values = sh.getRange(2, 1, last - 1, width).getValues();

  /* 같은 반·번호는 가장 최근 기록만 남긴다 */
  const latest = {};
  values.forEach(function (r) {
    const cell = function (i) { return i < r.length ? r[i] : ''; };
    const key = cell(1) + '-' + cell(2);
    const t = stamp_(cell(0));
    if (!latest[key] || t >= latest[key].t) {
      const row = {
        t: t,
        when: when_(cell(0)),
        klass: cell(1), number: cell(2), nick: cell(3),
        caught: cell(4), badges: cell(5), asked: cell(6), right: cell(7), rate: cell(8),
        hints: cell(12), wrongs: cell(13), battles: cell(14),
        stage: cell(15)
      };
      Object.keys(TOPIC_COL).forEach(function (k) { row[k] = cell(TOPIC_COL[k]); });
      latest[key] = row;
    }
  });

  const rows = Object.keys(latest).map(function (k) { return latest[k]; });
  rows.sort(function (a, b) {
    if (b.caught !== a.caught) return b.caught - a.caught;
    return (b.rate || 0) - (a.rate || 0);
  });
  return json_({ ok: true, rows: rows });
}
```

### 3단계. 배포

Apps Script 화면 오른쪽 위 배포 → 새 배포를 누르고 아래처럼 고릅니다.

| 항목 | 고를 것 |
|---|---|
| 유형 | 웹 앱 (톱니바퀴 아이콘에서 고름) |
| 설명 | 아무거나 (예: AI몬스터 기록) |
| 다음 사용자로 실행 | 나 |
| 액세스 권한 | 모든 사용자 |

배포를 누르면 권한을 묻습니다. 구글이 "확인되지 않은 앱" 경고를 띄우면 고급 → 안전하지 않음으로 이동을 눌러 진행합니다. 선생님이 직접 붙여넣은 코드라서 나오는 경고입니다.

끝나면 `https://script.google.com/macros/s/...../exec` 꼴의 웹 앱 URL이 나옵니다. 이 주소를 복사해 둡니다. 주소가 `/dev`로 끝나면 선생님만 열리므로 학생 기록이 오지 않습니다.

### 4단계. 게임에 주소 넣기

`data/config.js`를 열어 `SHEET_ENDPOINT`에 주소를 붙여넣고 저장합니다.

```js
const SHEET_ENDPOINT = "여기에 붙여넣기";
```

GitHub에 올려 쓰는 경우에는 커밋하고 올린 뒤 1~2분 기다리면 학생 화면에 반영됩니다.

```bash
cd "G:/내 드라이브/00_코딩/06_AI몬스터" && git add data/config.js && git commit -m "시트 주소 설정" && git push
```

### 5단계. 확인

1. 브라우저 주소창에 `웹앱주소?action=list&pw=아무말`을 넣어 엽니다. `"version":"v4"`와 `wrong password`가 보이면 창구가 새 코드로 돌고 있습니다.
2. 게임을 열어 반·번호·별명을 적고 몬스터를 한 마리 정화합니다.
3. 시트를 새로 고치면 줄이 하나 생겨 있습니다.
4. 타이틀 화면 맨 아래 작은 [선생님]을 누르고 암호를 넣으면 반 전체 표가 나옵니다.

---

## 이미 쓰고 계셨다면: v4로 바꾸기

2스테이지와 3스테이지 주제(편향·의존·조작·감시·불신·소외)와 학생이 지금 있는 스테이지는 v4 창구부터 시트에 남습니다. 옛 창구(v2·v3)를 그대로 두어도 게임은 멈추지 않지만, 선생님 화면에는 1스테이지 주제만 나옵니다. 2026년 9월 13일에 확인한 창구는 v2였습니다.

1. 시트에서 확장 프로그램 → Apps Script를 엽니다.
2. 지금 코드의 `TEACHER_PASSWORD` 줄에서 암호를 따로 적어 둡니다.
3. 코드를 모두 지우고 위 2단계 코드를 붙여넣은 뒤, 적어 둔 암호를 같은 줄에 다시 넣고 저장합니다.
4. 배포 → 배포 관리 → 연필 모양(편집) → 버전에서 "새 버전"을 고르고 배포를 누릅니다. 새 배포를 만들면 주소가 바뀌므로, 이미 쓰던 배포를 편집해 새 버전을 올립니다.
5. 5단계 1번처럼 주소를 열어 `"version":"v4"`가 보이는지 확인합니다.

저장만 하고 새 버전으로 배포하지 않으면 예전 코드가 계속 돕니다. 쌓여 있던 줄은 그대로 두어도 됩니다. 첫 기록이 들어올 때 제목 줄 뒤에 새 칸 이름이 붙습니다.

---

## 30명이 한꺼번에 보내면

기록은 몬스터를 정화할 때와 기록 화면에서 [시트로 보내기]를 누를 때 나갑니다. 한 스테이지에는 몬스터가 7마리이므로, 30명 한 반이 한 차시에 한 스테이지를 끝까지 하면 30 × 7 = 210번 보냅니다. 세 스테이지를 모두 하면 한 반에 630번쯤입니다.

구글 공식 문서(Apps Script 할당량, 2026년 9월 3일 갱신)는 스크립트 1회 실행 시간을 6분, 한 사용자의 동시 실행을 30개, 한 스크립트의 동시 실행을 1,000개로 적고 있습니다. 창구는 "나(선생님)"로 실행되므로 학생들이 보낸 기록이 모두 선생님 몫의 동시 실행 30개를 나눠 씁니다. 기록 한 줄을 붙이는 일은 짧게 끝나므로, 내 판단으로는 30명이 거의 같은 순간에 정화하지 않는 한 이 한도에 닿지 않습니다. 일일 실행 횟수 한도는 이 문서에 따로 적혀 있지 않았습니다.

넘어서 한 번 못 보내도 괜찮습니다. 기록은 매번 그때까지의 누적 값을 보내므로 다음 정화 때 따라잡습니다. 출처: https://developers.google.com/apps-script/guides/services/quotas

---

## 선생님 화면에서 보이는 것

같은 반·번호는 가장 최근 줄만 나옵니다. 시트에는 모든 줄이 남으므로 날짜별 변화도 볼 수 있습니다.

| 칸 | 뜻 |
|---|---|
| 있는 곳 | 학생이 지금 있는 스테이지 |
| 정화 | 정화한 몬스터 수 (세 스테이지 합해 21마리까지) |
| 주제 칸 | 그 주제 문제의 정답률. 아직 안 푼 주제는 "-" |
| 열쇠 모양 칸 | 생각 열쇠를 쓴 문제 수. 전에 틀린 문제는 열쇠가 저절로 펼쳐지므로 함께 셉니다 |

---

## 학년이 바뀌면

시트의 2행부터 마지막 행까지 지웁니다. 1행 제목 줄은 남겨 둡니다. 반과 번호가 새로 정해지므로 지우지 않으면 작년 학생과 올해 학생이 섞입니다.

교실 컴퓨터에 남은 기록은 타이틀 화면의 학생 목록에서 [지움]으로 지우거나, 브라우저의 사이트 데이터를 지우면 사라집니다.

---

## 안 될 때

시트에 아무것도 안 쌓이면 `data/config.js`의 주소가 `/exec`로 끝나는지, 배포할 때 액세스 권한을 "모든 사용자"로 골랐는지 봅니다. 학교망에서 `script.google.com`이 막혀 있는 경우도 있습니다. 그때도 게임은 멈추지 않고 그 컴퓨터에만 기록을 남깁니다.

선생님 화면에 "암호가 맞지 않아요"가 뜨면 Apps Script의 `TEACHER_PASSWORD`와 넣은 암호가 같은지 봅니다. 코드를 고친 뒤 새 버전으로 배포하지 않았을 수도 있습니다.

선생님 화면에 2·3스테이지 주제가 안 나오면 창구가 v4인지 5단계 1번으로 확인합니다.

연동을 잠시 끄려면 `data/config.js`의 `SHEET_ENABLED`를 `false`로, 아예 쓰지 않으려면 `SHEET_ENDPOINT`를 빈 문자열로 둡니다.

---

## 수업을 시작할 때 한마디

게임 첫 화면에는 "이름은 적지 않아요. 왜 그럴까요?"라는 물음이 있습니다. 이 물음으로 수업을 열어 볼 수 있습니다.

> "오늘 푼 기록은 선생님 시트로 가요. 그런데 이름은 안 보내고 반·번호·별명만 보내요. 왜 그렇게 만들었을까요?"
